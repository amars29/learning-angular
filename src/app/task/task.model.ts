export type TypeTask = {
  id: string;
  userId: string;
  title: string;
  summary: string;
  dueDate: string;
};

export type TypeNewTaskData = {
  title: string;
  summary: string;
  date: string;
};
